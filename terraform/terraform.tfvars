# AMI ID from Packer build (extracted from packer/packer-manifest.json)
ami_id = "ami-0b1e0bbb1f860a17f"

# Override instance type to t3.micro (also free tier eligible)
instance_type = "t3.micro"

# Using defaults for:
# - aws_region: us-east-1
# - private_instance_count: 6
# - my_ip: auto-detected
